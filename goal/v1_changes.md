1. Isolated web worker based for each benchmark
2. store benchmark history in ls api locally
3. check if browser supports web assembly or not. just a simple check
4. mobile throtling warning.
5. Add dark light mode capabilities.
6. Add search bar at the top and a side bar for proper segregation.
7. save configs to ls api for benchmarks.
8. make the inputs number only so unsafe code cannot be executed at all. (for secuirty and sanitation.)
9. Remove non native uuid tests and replace with a note in notes section.
10. Fix AES benchmark reusing same IV (we should use random iv)
11. add warning on top if device is offline or online. 
